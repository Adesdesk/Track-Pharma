import React from 'react';
export declare type CustomQRCodeProps = {
    value?: string;
    image?: React.ReactNode;
    imageBackground?: string;
    imagePosition?: 'center' | 'bottom right';
    tooltipMessage?: React.ReactNode | string;
};
