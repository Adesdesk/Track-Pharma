import { ReactNode } from 'react';
import { SIWEConfig } from './SIWEContext';
declare type Props = SIWEConfig & {
    children: ReactNode;
};
export declare const SIWEProvider: ({ children, enabled, nonceRefetchInterval, sessionRefetchInterval, signOutOnDisconnect, signOutOnAccountChange, signOutOnNetworkChange, ...siweConfig }: Props) => JSX.Element;
export {};
