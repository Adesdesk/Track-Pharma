import React from 'react';
import { All } from './../../types';
declare type NetworkButtonProps = {
    hideIcon?: boolean;
    hideName?: boolean;
};
declare const NetworkButton: React.FC<NetworkButtonProps & All>;
export default NetworkButton;
